@model List<QuestionAnswerVM>
@{
    Layout = "~/Areas/Admin/Views/_AdminLayout.cshtml";
}

<h1>Question Answers</h1>
<a class="modal-open btn btn-success" asp-action="AddEdit" asp-controller="QuestionAnswer">Add</a>
<table class="table">
    <thead>
        <tr>
            <th>#</th>
            <th>Text</th>
            <th>Is Correct</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody class="table-border-bottom-0">
        @foreach (var item in Model)
        {
            <tr>
                <td><strong>@item.Id</strong></td>
                <td><i class="fab fa-angular fa-lg text-danger me-3"></i> <strong>@item.Text</strong></td>
                <td>@item.IsCorrect</td>
                <td>
                    <div class="dropdown">
                        <button type="button" class="btn p-0 dropdown-toggle hide-arrow" data-bs-toggle="dropdown" aria-expanded="false">
                            <i class="bx bx-dots-vertical-rounded"></i>
                        </button>
                        <div class="dropdown-menu" style="">
                            <a asp-action="AddEdit" asp-route-id="@item.Id" asp-controller="QuestionAnswer" class="modal-open dropdown-item"><i class="bx bx-edit-alt me-1"></i>Edit</a>
                            <a class="dropdown-item" href="javascript:void(0);"><i class="bx bx-trash me-1"></i> Delete</a>
                        </div>
                    </div>
                </td>
            </tr>
        }
    </tbody>
</table>